{
  my = {

    shell.enable = true;

    packages.common.enable = true;

    core.git.enable = true;

    cli = {
      tmux.enable = true;
      superfile.enable = true;
    };

    dev.nvim.enable = true;

  };
}
